// ============================================
// GARAPEXPRESS THEME SYSTEM
// ============================================

// Export all theme modules
export { colors } from './colors';
export { spacing, borderRadius } from './spacing';
export { fontSize, fontWeight, typography } from './typography';
export { shadows, layout, animation } from './shadows';

// Import for default export
import { colors } from './colors';
import { spacing, borderRadius } from './spacing';
import { fontSize, fontWeight, typography } from './typography';
import { shadows, layout, animation } from './shadows';

export default {
  colors,
  spacing,
  borderRadius,
  fontSize,
  fontWeight,
  shadows,
  layout,
  typography,
  animation,
};